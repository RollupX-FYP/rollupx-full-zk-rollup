\section{Analytical Cost Model}
\label{sec:cost_models}
The empirical benchmarking uses static configurations swept explicitly to gather data on cost behavior. While theoretical "dynamic batching cost models" (which algorithmically predict optimal batch sizes based on fluctuating L1 base fees) are discussed in early project proposals, they are analytical constructs and are explicitly not implemented in the active runtime codebase.

Following the methodology of Chaliasos et al. \cite{chaliasos2024analyzing}, the cost model utilized for analyzing results splits into fixed batch costs (L1 settlement base fees, proof verification) and marginal transaction costs (DA bytes, witness generation, execution). 

\begin{table}[H]
\centering
\caption{ZK-Rollup Cost Model}
\label{tab:zk_rollup_cost_model}
\compacttable
\begin{tabularx}{\textwidth}{@{}p{0.22\textwidth}Y p{0.22\textwidth}p{0.18\textwidth}@{}}
\toprule
\textbf{Cost category} & \textbf{Examples} & \textbf{Fixed / marginal} & \textbf{Source} \\
\midrule
L1 settlement & Commit batch and pay base transaction fee & Fixed per batch & Chaliasos et al.~\cite{chaliasos2024analyzing} \\
Proof verification & Groth16 pairing check or verifier contract execution & Fixed per batch & Chaliasos et al.~\cite{chaliasos2024analyzing} \\
Data availability & Calldata bytes, blob bytes, or external DA commitment & Payload-dependent marginal cost & Saif et al.~\cite{saif2024dataavailability} \\
Proving time & Witness generation and proof generation & Marginal / cycle-dependent & Chaliasos et al.~\cite{chaliasos2024analyzing} \\
\bottomrule
\end{tabularx}
\end{table}
\normalsize

The fundamental equation governing Layer-1 gas cost per transaction is:
$$ \text{Cost per tx} = \left(\frac{\text{fixed batch settlement \& proof cost}}{\text{batch size}}\right) + \text{marginal DA cost per tx} $$
Larger batches reduce the amortized fixed cost per transaction but inherently increase queueing latency. 

It is important to note that the USD cost metrics reported in this document are derived from this gas model combined with assumed static environment parameters (e.g., \texttt{ETH\_PRICE\_USD} = \$2500, \texttt{REGULAR\_GAS\_PRICE\_GWEI} = 2.0). However, due to L1 base fee fluctuations, gas units (e.g., L1 Gas Used per batch) remain the only stable, comparable metric for cross-configuration efficiency evaluations.

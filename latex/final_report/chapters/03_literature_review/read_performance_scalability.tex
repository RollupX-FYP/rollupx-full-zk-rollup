\subsection{Read-Performance Scalability}
As the blockchain grows append-only, the sheer volume of data makes it difficult for lightweight nodes to query the state (Read-Performance). ZK-Rollups inadvertently exacerbate the read-performance issue on L2. To reconstruct the L2 state, nodes must parse massive amounts of compressed transaction data or state diffs posted to L1, a process that is resource-intensive.
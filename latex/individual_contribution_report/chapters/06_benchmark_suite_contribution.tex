\chapter{BENCHMARK SUITE CONTRIBUTION}

\section{Benchmark Suite Purpose}

The benchmark suite was developed to convert the research-grade prototype into a repeatable experimental framework. Since the project evaluates how different rollup configurations affect performance, manual execution of experiments would have been error-prone and difficult to reproduce. To ensure reproducible pipeline operations, I automated the benchmarking suite. The flow proceeds through: \texttt{experiments.toml} $\rightarrow$ \texttt{run\_matrix.sh} $\rightarrow$ \texttt{run\_experiment.sh} $\rightarrow$ Docker services $\rightarrow$ readiness wait $\rightarrow$ workload generation $\rightarrow$ metrics extraction $\rightarrow$ data-tools aggregation.

My contribution in this area focused on the initial benchmark orchestration layer, summarized in Table \ref{tab:automation_scripts}. This included the experiment configuration file, the scripts used to run individual experiments, the scripts used to execute a full experiment matrix, service readiness handling, and output collection. These components allowed the same benchmark procedure to be repeated across different data availability modes, workload rates, batch configurations, and sequencing policies.

Figure \ref{fig:benchmark_workflow} visualizes the benchmark automation workflow, showing how the scripts and configurations connect to produce the final evaluation data. This figure is reused from the final report to demonstrate the context of my orchestration scripts.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{../final_report/figures/chapter3/experiment_pipeline.pdf}
\caption{Experiment pipeline diagram (reused from final report), illustrating the sequence from TOML configuration parsing and Docker lifecycle management down to metric generation. I developed the scripts that coordinate this automated procedure.}
\label{fig:benchmark_workflow}
\end{figure}

\begin{table}[!htbp]
\centering
\caption{Benchmark Automation Scripts}
\label{tab:automation_scripts}
\small
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabularx}{\textwidth}{|
>{\raggedright\arraybackslash}p{0.24\textwidth}|
>{\raggedright\arraybackslash}p{0.28\textwidth}|
>{\raggedright\arraybackslash}X|}
\hline
\textbf{Script/File} &
\textbf{Role} &
\textbf{Why It Was Needed} \\ \hline

\path{run_matrix.sh} &
Iterates through multiple benchmark configurations and invokes individual experiment runs. &
Automates testing across the experiment space, reducing manual execution errors and improving reproducibility. \\ \hline

\path{run_experiment.sh} &
Handles the lifecycle of one benchmark run, including service setup, workload execution, and metric collection. &
Ensures each run starts from a clean state and produces consistently organized output files. \\ \hline

\path{wait_for_sequencer.sh} &
Waits until the Sequencer API is ready before workload generation begins. &
Prevents premature transaction submission and avoids failed or incomplete benchmark runs caused by service startup timing. \\ \hline

\end{tabularx}
\end{table}



The benchmark suite was important because the final research results depended on consistent experimental execution. If service startup, configuration injection, workload timing, or metrics collection were inconsistent, the measured throughput, latency, finalization time, and cost values would not be reliable.

\section{Experiment Matrix}

The experiment matrix was defined in \path{benchmark-suite/config/experiments.toml}, with commit evidence from \texttt{9b282f0}. This file provides the configurable input space for benchmark execution. Instead of hardcoding experiment settings inside shell scripts, the TOML file defines the parameters that the orchestration layer can iterate over.

The configuration captures experiment variables such as data availability mode, workload rate, batch sizes, sequencing policies, and repeats. 

Furthermore, the workload can maintain an 8 TPS base rate and periodically inject an 80 TPS burst, a configuration verified and heavily analyzed during the benchmark stages.

Exact excerpt from \path{benchmark-suite/config/experiments.toml}:
\begin{lstlisting}[language=bash, numbers=left, breaklines=true, basicstyle=\ttfamily\small]
[baseline]
id = "exp_000_baseline_fixed100_calldata_fcfs_10tps"
name = "Baseline: fixed batch size 100, calldata DA, FCFS, 10 TPS"
batch_size = 100
timeout_ms = 30000
min_batch_size = 1
batch_policy = "fixed"
policy = "FCFS"
da_mode = "calldata"
rate_tps = 10
repeats = 5
seeds = [42, 43, 44, 45, 46]
\end{lstlisting}

The technical value of this design is the strict separation between experiment definition and experiment execution. This TOML structure drives the underlying experiments systematically. The benchmark scripts do not need to be manually edited for each test case. Instead, the orchestration logic parses these configurations and executes the corresponding runs, significantly improving the repeatability of our data collection.

\section{Run Scripts and Orchestration}

I implemented the benchmark execution scripts that coordinate the local test environment and automate experiment runs. The main scripts include:

\begin{itemize}
    \item \path{benchmark-suite/scripts/run_experiment.sh}: executes one benchmark configuration from environment setup to metric extraction.
    \item \path{benchmark-suite/scripts/run_matrix.sh}: iterates through multiple experiment configurations and repeat runs defined by the benchmark matrix.
    \item \path{benchmark-suite/scripts/wait_for_sequencer.sh}: waits until the Sequencer service is ready before workload generation begins.
\end{itemize}

These scripts provide the operational glue between Docker services, configuration files, workload generation, and metrics output. They are therefore part of the integration layer of the project, not just helper scripts.

\section{Code-Level Implementation: \texttt{run\_experiment.sh}}

The \path{benchmark-suite/scripts/run_experiment.sh} script is responsible for executing a single benchmark run in a controlled environment. Its role is to make sure that the services are started in the correct order, the workload begins only after the system is ready, and the metric outputs are copied into a stable results directory.

At a code-flow level, the script explicitly handles the Docker service lifecycle by stopping prior runs, injecting the necessary parameters, and bringing the stack back up. This prevents stale states. 

Exact excerpt from \path{benchmark-suite/scripts/run_experiment.sh}:
\begin{lstlisting}[language=bash, numbers=left, breaklines=true, basicstyle=\ttfamily\small]
docker compose --profile core down -v --remove-orphans

if [[ "${DOCKER_UP_BUILD:-1}" == "1" || "${DOCKER_UP_BUILD:-1}" == "true" ]]; then
    METRICS_DIR="$metrics_abs" \
    EXPERIMENT_ID="$EXP_ID" \
    EXPERIMENT_NAME="$EXPERIMENT_NAME" \
    docker compose --profile core up -d --force-recreate --build
else
    # ... fallback ...
    docker compose --profile core up -d --force-recreate
fi
\end{lstlisting}

Following the start, the script invokes the readiness checks, triggers the workload generator, and finally copies the resulting metrics out. This script was essential for benchmark reliability because it encoded the execution procedure as repeatable automation. Without it, each experiment would require manual startup, manual workload triggering, and manual metric collection, increasing the risk of inconsistent results.

\section{Code-Level Implementation: \texttt{run\_matrix.sh}}

The \path{benchmark-suite/scripts/run_matrix.sh} script extends the single-run workflow into a full experiment sweep. Its purpose is to execute multiple benchmark configurations in sequence and organize their outputs consistently.

At a high level, the matrix runner iterates over the configurations array provided by the TOML parser:

Exact excerpt from \path{benchmark-suite/scripts/run_matrix.sh}:
\begin{lstlisting}[language=bash, numbers=left, breaklines=true, basicstyle=\ttfamily\small]
for exp in cfg["experiments"]:
    factor = exp.get("factor")
    if filter_factor in (None, "", "all"):
        pass
    elif filter_factor == "workload" and factor not in ("rate", "tx_mix"):
        continue
    elif filter_factor != "workload" and factor != filter_factor:
        continue
    experiments_to_run.append({**baseline, **exp})
\end{lstlisting}

It then loops over \texttt{experiments\_to\_run}, invoking \path{run_experiment.sh} and preventing stale outputs from contaminating succeeding runs. This script directly supported the empirical evaluation because it allowed the same benchmark procedure to be repeated over many configurations automatically.

\section{Benchmark Bug Fix: Sequencer Readiness Wait}

One important reliability issue in the early benchmark workflow was service startup timing. The Docker containers could be started successfully, but the Sequencer HTTP API might not yet be ready to accept incoming transactions. If the workload generator began sending requests during this short startup window, the benchmark run could fail or produce incomplete measurements.

To address this race condition, I introduced the readiness script \path{benchmark-suite/scripts/wait_for_sequencer.sh} in commit \texttt{9b282f0}. The script polls the Sequencer health endpoint or API endpoint until the service is confirmed to be ready. Only after this readiness check passes does the benchmark workflow continue to workload generation.

Exact excerpt from \path{benchmark-suite/scripts/wait\_for\_sequencer.sh}:
\begin{lstlisting}[language=bash, numbers=left, breaklines=true, basicstyle=\ttfamily\small]
for i in $(seq 1 "$MAX"); do
    if curl -sf "http://$HOST:$PORT/health" > /dev/null 2>&1; then
        exit 0
    fi
    sleep 2
done
\end{lstlisting}
This service readiness check resolved a critical race condition between the Docker container lifecycle and the internal application initialization.

The root cause of the issue was the difference between container startup and application readiness. Docker can report that a container is running even though the application inside the container has not completed initialization. The readiness wait script fixed this by checking the actual service endpoint rather than assuming that a running container means a ready Sequencer.

This fix improved benchmark reproducibility. It reduced failed runs caused by premature workload submission and made the measured latency and throughput values more reliable because experiments started from a known-ready system state.

\section{Benchmark Output and Metric Collection}

The benchmark suite also defines how outputs are collected after execution. Each run produces service-level metrics from components such as the Sequencer, Executor, Prover, and Submitter. These metrics are written as raw files, typically in JSON, JSONL, or CSV form, and copied into a result directory after each benchmark run.

The output structure matters because the data-tools pipeline expects consistent file locations and metric field names. By standardizing where results are stored, the benchmark suite allowed later scripts such as \path{data-tools/aggregate.py} and \path{data-tools/stats.py} to process results without manual reorganization.

\section{Technical Impact}

My benchmark suite contribution provided the automation needed to evaluate RollupX as a research framework. The experiment matrix defined what should be tested, \path{run_experiment.sh} executed individual runs, \path{run_matrix.sh} repeated the process across configurations, and \path{wait_for_sequencer.sh} improved startup reliability.

Together, these scripts connected the implementation modules into a reproducible experimental workflow. This allowed the project to measure the effect of different data availability modes, workload intensities, and batching configurations using a consistent execution procedure. The benchmark suite therefore formed the foundation for the empirical results presented in the final project report.

